export enum TxStatus {
  pending = 'pending',
  broadcast = 'broadcast',
  mined = 'mined',
  failed = 'failed',
}

export enum OnchainTxStatus {
  pending = 'pending',
  mined = 'mined',
  failed = 'failed',
}
